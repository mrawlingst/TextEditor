extends Node

var map: Dictionary = {
    # SPC
    "key_space": {
        # SPC w
        "key_w" : {
            # SPC w -
            "key_minus": { "func": "NYI", "desc": "split window vertically" },
            
            # SPC w /
            "key_slash": { "func": "NYI", "desc": "split window horizontally" },
        },
    },
}
